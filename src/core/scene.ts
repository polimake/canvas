import type { ExcalidrawInitialDataState, ExcalidrawImperativeAPI } from './excal';

/** Shared contracts; the core does not depend on the React component. */
export type Canvas2Scene = ExcalidrawInitialDataState;
export type Canvas2Api = ExcalidrawImperativeAPI;
